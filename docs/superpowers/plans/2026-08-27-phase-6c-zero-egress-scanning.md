# Phase 6C Zero-Egress Repository Scanning Implementation Plan

Date: 2026-08-27
Design: `docs/superpowers/specs/2026-08-27-phase-6c-zero-egress-scanning-design.md`
Branch: `feat/phase-6c-zero-egress-scanning`

## Execution rules

- Do not trigger, rerun, or depend on GitHub Actions.
- Every implementation/documentation commit uses `[skip ci]`.
- Use test-first commits where practical.
- Never claim Vitest/typecheck/build/sandbox gates are green unless they actually run.
- Existing deployed migrations are immutable. Phase 6C database work is forward-only.
- Keep Phase 6C product enablement closed until a real rootless-Podman/cgroup-v2 worker passes sandbox verification.

## Task 1 - Lock the Phase 6C worker contract

### Tests first

Create:

- `tests/workers/repository-scan-contracts.test.ts`
- extend `tests/workers/database-types.test.ts`

Assert:

- execution class `phase3_repository_scan_no_egress_v1`
- `networkPolicy = none`
- exact fixed Phase 6C budget
- broker input contains only immutable snapshot/profile provenance
- broker input cannot contain URLs/object keys/commands/images/env/rules/network configuration
- successful terminal result is the privacy-reduced hosted envelope plus exact snapshot/result identity
- output is capped at 3.5 MiB
- closed failure-code set includes Phase 6C failures

Expected RED command when executable:

```text
npx vitest run tests/workers/repository-scan-contracts.test.ts tests/workers/database-types.test.ts
```

### Implementation

Modify:

- `packages/worker-contracts/types.ts`
- `packages/worker-contracts/profiles.ts`
- `packages/worker-contracts/validation.ts`
- `lib/database.types.ts` after live schema reconciliation, not before migrations exist

Use profile:

```text
wall/cpu 300000 ms
memory 1073741824
pids 64
input files 20000
input bytes 268435456
scratch 268435456
output 3670016
network none
```

Commit:

```text
feat: define Phase 6C worker contracts [skip ci]
```

## Task 2 - Add strict Phase 6B snapshot bundle consumption

### Tests first

Create:

- `tests/repository-snapshots/reader.test.ts`
- `tests/repository-snapshots/reader-hostile.test.ts`

Cover:

- valid deterministic Phase 6B bundle
- exact artifact digest/byte count
- manifest schema/format/content digest
- exact repository URL/commit identity
- exact file set/size/SHA-256
- long-path PAX generated by ScopeForge
- traversal/absolute/backslash/NUL path rejection
- duplicate/shadowing rejection
- link/device/FIFO/special-entry rejection
- unexpected PAX/global PAX rejection
- extra file missing from manifest
- manifest entry missing from archive
- tampered file
- truncated/oversized gzip/tar
- manifest never materialized in source root

### Implementation

Add:

- `packages/repository-snapshot/reader.ts`
- export from `packages/repository-snapshot/index.ts`

Reader writes only regular files beneath a fresh caller-provided work directory, create-exclusive, then changes files/directories to read-only after verification.

Commit:

```text
feat: verify and materialize immutable snapshots [skip ci]
```

## Task 3 - Extract shared deterministic hosted-result normalization

### Tests first

Create:

- `tests/phase3-results/normalization.test.ts`

Verify Phase 5C and Phase 6C can share finding/evidence identity mapping without changing existing source kind, rule refs, privacy classification, secret-column suppression, remediation mapping, or lifecycle authority.

### Implementation

Add:

- `lib/phase3-results/normalization.ts`
- `lib/phase3-results/types.ts` if needed

Refactor:

- `lib/phase3-import/service.ts`

Keep Phase 5C behavior byte/semantics compatible.

Commit:

```text
refactor: share deterministic Phase 3 persistence mapping [skip ci]
```

## Task 4 - Build the fixed hosted scanner runner

### Tests first

Create:

- `tests/repository-scans/runner.test.ts`
- `tests/repository-scans/runner-architecture.test.ts`

Assert:

- fixed built-ins only: secrets, jsts, sca, iac
- OSV remote lookup disabled
- repository scanner config is not loaded
- no baseline/SBOM/fail-on/output config path
- existing inventory limits are used
- scanner errors produce runner failure rather than publishable partial success
- output uses existing hosted privacy reducer
- maximum 500 findings
- runner graph has no network/process/worker-thread/VM/Supabase/R2/model/runtime imports

### Implementation

Add:

- `packages/hosted-scanner-runner/profile.ts`
- `packages/hosted-scanner-runner/run.ts`
- `packages/hosted-scanner-runner/index.ts`
- `packages/hosted-scanner-runner/container-entry.ts`

Do not invoke `packages/cli/run-cli.ts`.

Commit:

```text
feat: add fixed zero-egress hosted scanner runner [skip ci]
```

## Task 5 - Implement rootless Podman sandbox contract and adapter

### Tests first

Create:

- `tests/worker-sandbox/podman-command.test.ts`
- `tests/worker-sandbox/podman-runtime.test.ts`
- `tests/worker-sandbox/architecture.test.ts`

Command tests require exactly:

```text
--pull=never
--network=none
--read-only
--read-only-tmpfs=false
--cap-drop=all
--security-opt=no-new-privileges
--pids-limit=64
--memory=1g
--memory-swap=1g
--cpus=1
--log-driver=none
--user=<fixed non-root uid:gid>
read-only /workspace bind mount
bounded /tmp tmpfs
bounded /result tmpfs
```

Also require:

- immutable image digest syntax only
- absolute validated Podman binary path
- no shell command strings
- no caller/task fields can add arguments
- `--pull=never` so missing images fail instead of registry egress
- no privileged/device/socket/host-network mounts

Runtime tests with fake process adapter require abort to issue kill then remove and not settle until termination cleanup completes.

### Implementation

Add:

- `packages/worker-sandbox/types.ts`
- `packages/worker-sandbox/podman-command.ts`
- `packages/worker-sandbox/podman-runtime.ts`
- `packages/worker-sandbox/index.ts`

Only this package may import `node:child_process` for Phase 6C.

Commit:

```text
feat: add rootless Podman scanner sandbox [skip ci]
```

## Task 6 - Add Phase 6C database schema and enqueue contract

### Tests first

Create:

- `tests/repository-scans/migration.test.ts`
- `tests/repository-scans/control-migration.test.ts`

Assert SQL contains:

- `repository_scan` scan-job enum value
- Phase 6C class in worker node/task constraints
- fixed scan-job budget and null runtime authorization/profile fields
- immutable public `repository_scan_runs`
- private `repository_scan_tasks` exact snapshot/job/workspace/asset/actor binding
- owner/admin enqueue
- newest eligible snapshot selected server-side
- snapshot artifact active and at least 30 minutes from expiry
- 5-minute cooldown
- 20/day workspace cap
- one active repository scan per workspace
- covering indexes for every FK
- RLS member SELECT only
- no direct public/anon/authenticated/service-role mutation on run provenance/private task state

### Implementation

Add forward migrations with ordered timestamps after existing Phase 6B files, for example:

- `supabase/migrations/20260827020000_phase_6c_repository_scan_enum.sql`
- `supabase/migrations/20260827020100_phase_6c_repository_scan_schema.sql`
- `supabase/migrations/20260827020200_phase_6c_repository_scan_enqueue.sql`

Do not apply them to production until source review is complete for the migration set.

Commit:

```text
feat: add Phase 6C scan queue schema [skip ci]
```

## Task 7 - Add exact-lease snapshot staging authorization

### Tests first

Create:

- `tests/repository-scans/artifact-access.test.ts`
- `tests/repository-scans/artifact-route.test.ts`
- `tests/repository-scans/r2-get-signing.test.ts`

Assert:

- only exact active Phase 6C worker/task/attempt/lease can request staging access
- DB resolves object key from task snapshot, never from worker/browser input
- only active, non-deleted source artifact is eligible
- descriptor expires <=120 seconds
- GET only, exact R2 host/path, HTTPS/443, zero redirects
- response does not expose long-lived R2 credentials
- foundation/acquisition workers cannot obtain scan artifact descriptors

### Implementation

Modify/add:

- `lib/repository-snapshots/r2-signature-v4.ts` for presigned GET
- `lib/repository-scans/repository.ts`
- `lib/repository-scans/artifact-access.ts`
- `app/api/internal/workers/repository-scans/artifact/route.ts`
- Phase 6C artifact-resolution RPC migration

Commit:

```text
feat: add lease-bound snapshot staging access [skip ci]
```

## Task 8 - Implement trusted host artifact stager

### Tests first

Create:

- `tests/repository-scans/stager.test.ts`
- `tests/repository-scans/stager-policy.test.ts`

Cover:

- short-lived GET descriptor validation
- no redirects
- exact content length when present
- streamed max and exact final byte count
- whole-artifact SHA-256 equality
- strict bundle reader integration
- snapshot URL/commit/content digest equality
- read-only materialized source
- manifest excluded from source root
- cleanup on success/error/abort

### Implementation

Add:

- `packages/worker-supervisor/repository-scan-stager.ts`
- `packages/worker-supervisor/repository-scan-download.ts`

The stager is supervisor-owned and receives worker lease authority through trusted composition. The sandbox executor never receives the descriptor or lease.

Commit:

```text
feat: stage repository snapshots outside scanner sandbox [skip ci]
```

## Task 9 - Integrate prepared-input execution into supervisor

### Tests first

Extend/create:

- `tests/workers/service.test.ts`
- `tests/repository-scans/supervisor.test.ts`
- `tests/repository-scans/executor-architecture.test.ts`

Require:

- claim remains class-aware
- repository scan is prepared before executor
- executor receives only local read-only source path plus safe snapshot metadata
- lease token remains supervisor-only
- R2 descriptor remains stager-only
- cancellation during staging aborts download and cleans files
- cancellation during sandbox kills container
- deadline/control loss kills container
- generic success finalizer rejects Phase 6C success

### Implementation

Modify:

- `packages/worker-supervisor/supervisor.ts`
- `packages/worker-supervisor/executor.ts`
- `packages/worker-supervisor/control-client.ts`
- `packages/worker-supervisor/index.ts`
- `lib/worker-control/types.ts`
- `lib/worker-control/service.ts`
- `lib/worker-control/repository.ts`

Add:

- `packages/worker-supervisor/repository-scan.ts`

Commit:

```text
feat: execute prepared scans through isolated supervisor path [skip ci]
```

## Task 10 - Add dedicated Phase 6C result route and publication

### Tests first

Create:

- `tests/repository-scans/result-validation.test.ts`
- `tests/repository-scans/publication.test.ts`
- `tests/repository-scans/publication-migration.test.ts`
- `tests/repository-scans/finalize-route.test.ts`

Require:

- dedicated worker-authenticated finalize route with <=4 MiB streamed cap
- exact terminal task/attempt/class binding
- existing hosted Phase 3 schema validation
- exact repository/profile/scanner descriptor match
- `scannerErrorCount = 0`
- inventory within snapshot limits
- existing source registry accepts every finding
- server-derived run reference for persistence
- exact result digest
- exact lease/snapshot/job binding in RPC
- cancellation-first publication
- generic success finalizer rejection
- exact replay accepted
- conflicting replay rejected
- zero findings accepted without resolving prior findings
- scanner-error/invalid-output paths insert no canonical findings

### Implementation

Add/modify:

- `lib/repository-scans/result-validation.ts`
- `lib/repository-scans/service.ts`
- `app/api/internal/workers/repository-scans/finalize/route.ts`
- dedicated publication migration
- `lib/worker-control/http-response.ts` only if a route-specific bounded reader helper is needed

Reuse shared normalization from Task 3. Do not route through browser/user `phase3_import` persistence.

Commit:

```text
feat: publish hosted repository scan findings atomically [skip ci]
```

## Task 11 - Add server action, read model, and asset UX

### Tests first

Create:

- `tests/repository-scans/action.test.ts`
- `tests/repository-scans/read-model.test.ts`
- `tests/repository-scans/panel.test.tsx`

Assert:

- action accepts asset ID only
- user/workspace/role derived from authenticated server context
- owner/admin mutation only
- browser cannot select snapshot/class/profile/budget
- panel shows safe run provenance/status only
- no object key/URL/source/worker/lease/container/local-path data

### Implementation

Add:

- `lib/repository-scans/read-model.ts`
- `app/dashboard/assets/[assetId]/scan-actions.ts`
- `components/assets/RepositoryScanPanel.tsx`

Modify:

- `app/dashboard/assets/[assetId]/page.tsx`

Keep the request button disabled with an explicit runtime-unavailable state until Phase 6C production worker readiness is proven.

Commit:

```text
feat: add hosted repository scan workflow UI [skip ci]
```

## Task 12 - Add permanent authority regression guards

### Tests first

Create:

- `tests/repository-scans/architecture.test.ts`
- `tests/worker-sandbox/architecture.test.ts`

Extend:

- `tests/architecture/phase3-import-dependencies.test.ts`
- existing worker/repository-snapshot architecture guards

Forbid:

- hosted runner -> CLI config/command path
- hosted runner -> network/R2/GitHub/runtime/model/Supabase/process/worker-thread/VM
- sandbox -> scanner/Supabase/control client/runtime/GitHub/R2
- stager -> GitHub acquisition or R2 PUT
- acquisition worker -> scanner/sandbox
- Phase 5C -> worker/sandbox
- browser/components -> worker/stager/sandbox/object store
- foundation -> scanner/GitHub/R2/sandbox

Commit:

```text
security: lock Phase 6C authority boundaries [skip ci]
```

## Task 13 - Source review, then production database reconciliation

Before applying SQL:

1. Compare exact Phase 6C migration files against the design.
2. Check every `SECURITY DEFINER` function has `search_path = ''`.
3. Check explicit function/table privilege revocations.
4. Check direct `service_role` table privileges are absent where RPC-only authority is intended.
5. Check every FK has a covering index.
6. Check cancellation before publication insert.
7. Check replay/conflict semantics.

Then apply reviewed forward migrations to Supabase project `tdgpibrepzcvdivztkta` in order.

Directly verify:

- migration history
- enum values
- constraints/indexes/FKs
- RLS/policies
- ACLs
- function owner/security/search path/EXECUTE grants
- private helper execution
- security advisor
- performance advisor
- generated public TypeScript types

Any defect after deployment is fixed only by another forward migration.

Commit any live hardening plus tests as:

```text
fix: harden live Phase 6C database authority [skip ci]
```

## Task 14 - Rollback-only production workflow smoke

Inside one explicit rollback transaction, if production remains empty:

1. create synthetic user/workspace/owner/repository asset
2. create eligible published Phase 6B snapshot plus private artifact metadata
3. enqueue Phase 6C scan
4. register Phase 6C worker
5. claim exact class
6. resolve exact lease-bound artifact metadata
7. finalize one valid privacy-reduced finding
8. assert immutable run provenance + canonical finding/evidence occurrence
9. exact replay
10. conflicting replay rejection
11. cancellation-wins with no run/finding insert
12. zero-finding success leaves an existing canonical finding lifecycle unchanged
13. rollback
14. confirm zero synthetic rows

Do not fabricate real object-store bytes for this DB-only smoke.

## Task 15 - Real sandbox-runtime acceptance gate

On a Linux worker environment with rootless Podman and cgroup v2, prove:

```text
podman --version
podman info
```

Then run dedicated fixtures proving:

- `--pull=never` refuses missing image instead of registry access
- scanner container has no network
- source/rootfs are read-only
- memory/PID/CPU limits are accepted and active
- scratch/output tmpfs bounds
- image/entrypoint cannot be changed by repository input
- cancellation/deadline kills the actual container
- result remains privacy-reduced

If no such environment is available, keep hosted scan product enablement off and record the missing evidence in `TEST_STATUS.md`.

## Task 16 - Final exact-head review, PR, merge, and state reconciliation

Update:

- `docs/ARCHITECTURE.md`
- `docs/PHASES.md`
- `docs/development/CURRENT_STATE.md`
- `docs/development/NEXT_STEPS.md`
- `docs/development/SESSION_HANDOFF.md`
- `docs/development/TEST_STATUS.md`

Then:

1. re-check exact feature head
2. compare with `main`
3. open Phase 6C PR without Actions
4. inspect every changed file and security-sensitive patch
5. inspect submitted reviews and unresolved threads
6. fix blockers with `[skip ci]`
7. re-review new exact head after any fix
8. merge only with `expected_head_sha`
9. reconcile `main` docs after merge

Never claim full npm/build/sandbox success without executable evidence.
