# Accessibility Baseline

ScopeForge uses semantic structure, visible focus states and text labels alongside color signals. Interactive controls should remain keyboard accessible, and severity or status must never rely on color alone.

Responsive layouts are tested at narrow mobile widths as part of feature completion rather than after release.
